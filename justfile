dev:
    dx serve --platform linux --hot-patch --features hot-patch
dev-release:
    dx serve --platform linux --release --hot-patch --features hot-patch
release:
    dx serve --platform linux --release 
